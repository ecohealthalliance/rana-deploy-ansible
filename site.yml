---

- hosts: rana
  roles:
    - common
    - apache
    - mongodb
    - node
    - { role: rana, tags: ["rana"] }
    - { role: backup, tags: ["backup"] }
  vars_files:
    - secure.yml
  sudo: yes
